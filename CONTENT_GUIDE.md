# 아인산업안전 홈페이지 — 콘텐츠 수정 설명서

이 문서는 **개발 지식이 없어도** 홈페이지의 글·사진·시공사례를 직접 바꿀 수 있도록 만든 설명서입니다.

---

## 0-0. ⭐ 바뀐 점 — 시공사례가 기술자료로 합쳐졌습니다

상단 메뉴에서 **시공사례** 가 없어지고 **기술자료** 하나만 남았습니다.
기술자료 페이지에 들어가면 **기술문서와 시공사례가 함께** 나옵니다.

- 카드 왼쪽 위 배지로 구분됩니다 → **검은 배지 = 시공사례**, **흰 배지 = 기술자료**
- 위쪽 필터로 골라볼 수 있습니다
  - 카테고리: 전체 / 노출콘크리트 / 균열·보수 / 특수방수 / 안전시설·자재
  - 자료 유형: 전체 / 기술자료 / 시공사례
- 검색창에서 두 가지를 한 번에 찾을 수 있습니다.

> ✅ **기존에 만들어 둔 시공사례와 기술자료는 하나도 지워지지 않았습니다.**
> 예전에 공유한 주소(`project.html?id=…`, `resource.html?id=…`)도 전부 그대로 열립니다.
> 시공사례만 모아 보던 화면(`projects.html`)도 그대로 남아 있습니다.

**글을 추가하는 방법은 바뀌지 않았습니다.** 아래 설명 그대로 하시면 됩니다.

---

## 0. 가장 먼저 알아둘 것

- 홈페이지의 **모든 내용**은 딱 한 파일에 들어 있습니다 →
  ### 📄 `data/site-content.js`
- **이 파일 하나만** 고치면 됩니다. `index.html` 은 열지 마세요.
- 사진은 `assets/images` 폴더에 **같은 이름으로** 넣으면 바뀝니다.

### 글을 고칠 때 지켜야 할 3가지 규칙
1. 글자는 반드시 **작은따옴표 `'` 로 감쌉니다.** → `name: '아인산업안전',`
2. 줄 끝의 **쉼표 `,` 를 지우지 마세요.** (쉼표가 빠지면 화면이 깨집니다)
3. 줄을 바꾸고 싶으면 글 안에 **`<br>`** 를 넣습니다. → `'콘크리트,<br>다시 되살립니다'`

> ⚠️ 큰따옴표 `"` 나 특수문자 대신, 웬만하면 원래 있던 글자만 바꿔 쓰세요.

---

## 1. 메인 문구(첫 화면 큰 글자) 수정

`data/site-content.js` 에서 `hero:` 부분을 찾습니다.

```js
hero: {
  title: '콘크리트 본연의 표정을,<br>다시 되살립니다',   // ← 큰 제목
  en:    'Exposed Concrete Restoration',              // ← 제목 아래 영문
  sub:   '제주 노출콘크리트 면보수 · 색상 재현 · 균열 보수 전문', // ← 한 줄 설명
```

작은따옴표 안의 글자만 바꾸면 됩니다.

---

## 2. 회사 정보(전화·주소·이메일 등) 수정

`company:` 부분을 찾습니다.

```js
company: {
  name:      '주식회사 아인산업안전',
  shortName: '아인산업안전',              // 헤더 로고 이름
  tel:       '1660-4019',                // 화면에 보이는 번호
  telHref:   'tel:16604019',             // 전화 걸기용 (숫자만!)
  email:     'ainsafe@naver.com',
  address:   '제주특별자치도 서귀포시 성산읍 풍천로 142, 103호',
  blog:      'https://blog.naver.com/ainsafe',
  ...
```

> 📞 전화번호를 바꿀 때는 `tel` 과 `telHref` **둘 다** 바꿔야 합니다.
> `telHref` 에는 하이픈 없이 숫자만 적습니다. (`1660-4019` → `tel:16604019`)

전화번호·주소·이메일·블로그 주소는 여기 한 곳만 고치면 **홈페이지 전체(헤더·상담·푸터)** 에 자동 반영됩니다.

---

## 3. Hero 사진(첫 화면 배경) 변경

**방법 A — 가장 쉬움 (사진만 교체):**
1. 새 사진 파일 이름을 `hero-1.jpg`, `hero-2.jpg`, `hero-3.jpg` 로 바꿉니다.
2. `assets/images/hero/` 폴더에 넣어 기존 파일을 덮어씁니다.
3. 끝. (글은 안 건드려도 됩니다)

**방법 B — 사진을 늘리거나 줄이기:**
`hero:` 의 `images` 목록을 고칩니다.

```js
images: [
  { src: 'assets/images/hero/hero-1.jpg', alt: '설명 글' },
  { src: 'assets/images/hero/hero-2.jpg', alt: '설명 글' },
  { src: 'assets/images/hero/hero-3.jpg', alt: '설명 글' }
]
```
- **추가:** `{ ... }` 한 줄을 복사해 붙이고, 앞 줄 끝에 쉼표 `,` 를 확인합니다.
- **삭제:** 그 `{ ... }` 줄을 지웁니다. (사진이 1장만 남으면 자동 넘김이 멈춥니다)

---

## 4. 회사소개 사진 변경

- **회사소개 옆 사진:** `assets/images/about/about-1.jpg` 를 새 사진으로 덮어씁니다.
- **‘제주 현장 대응’ 옆 사진:** `assets/images/about/about-2.jpg` 를 덮어씁니다.

파일 이름을 그대로 두고 내용만 바꾸면 글은 고칠 필요가 없습니다.

---

## 5. 서비스(전문 분야) 수정

`services:` 목록을 고칩니다.

```js
services: [
  { no: '01', title: '노출콘크리트 보수', text: '설명...', link: 'concrete.html' },
  { no: '02', title: '인젝션 및 누수 보수', text: '설명...', link: 'waterproof.html' },
  ...
]
```
- 글자만 바꾸면 내용이 바뀝니다.
- 항목 **추가/삭제**: `{ ... }` 한 줄을 복사하거나 지웁니다.

---

## 6. 시공사례 추가 (가장 자주 쓰는 작업) ⭐

`works:` 목록에서 사례 하나(`{ ... }` 한 덩어리)를 **통째로 복사**해서,
**제목 / 설명 / 사진 / 공종**만 바꿉니다.

```js
works: [
  {
    title:       '별빛누리공원 노출콘크리트 복원',   // 제목
    description: '해풍과 자외선에 백화된 외벽을...',   // 설명
    image:       'assets/images/works/work-1.jpg',  // 사진 파일
    imageAlt:    '별빛누리공원 노출콘크리트 복원',     // 사진 설명(검색용)
    type:        '노출콘크리트',                      // 공종 (아래 9번 참고)
    link:        'projects.html'
  },
  ...
]
```

### 새 사례 추가 순서
1. 사진 파일을 `assets/images/works/` 에 넣습니다. (예: `work-4.jpg`)
2. `works:` 안의 `{ ... }` 한 덩어리를 복사해 **맨 위**에 붙입니다.
3. `title`, `description`, `image`, `type` 을 새 내용으로 바꿉니다.
4. 붙여넣은 덩어리 끝에 쉼표 `,` 가 있는지 확인합니다.

> 홈페이지 첫 화면에는 목록 **맨 위 3개**만 보입니다.
> (개수는 `worksSection` 의 `homeLimit: 3` 숫자로 조절)

---

## 7. 시공사례 삭제

지우고 싶은 사례의 `{ ... }` 덩어리를 **통째로** 지웁니다.
지운 뒤, 남은 덩어리들 사이 쉼표가 올바른지 확인합니다.

---

## 8. 시공사례 순서 변경

`{ ... }` 덩어리의 **위아래 위치**를 바꾸면 됩니다.
**위에 있을수록 먼저** 나옵니다.

---

## 9. 공종(工種) 수정

시공사례는 지역이 아니라 **‘무슨 공사인지(공종)’** 로 분류합니다.
각 사례의 `type:` 에는 아래 중 하나를 적습니다.

```
노출콘크리트 / 인젝션 / 특수방수 / 안전시설 / 기타
```

이 분류 목록 자체는 `workTypes:` 에서 관리합니다.

```js
workTypes: ['전체', '노출콘크리트', '인젝션', '특수방수', '안전시설', '기타'],
```

> 🚫 서울/제주/부산 같은 **지역 분류(메뉴·필터)는 만들지 않습니다.**
> 현장 위치가 필요하면 개별 시공사례 상세(`assets/js/projects.js`)의 `location` 항목에만 적습니다.

---

## 10. 이미지 권장 크기

| 위치 | 폴더 | 권장 크기 | 비율 | 형식 |
|---|---|---|---|---|
| Hero 배경 | `assets/images/hero/` | 가로 1600px 내외 | 가로형(3:2) | JPG |
| 회사소개 사진 | `assets/images/about/` | 900 × 1200px 내외 | 세로형(3:4) | JPG |
| 시공사례 사진 | `assets/images/works/` | 1200 × 900px 내외 | 가로형(4:3) | JPG |

- 용량은 한 장당 **300KB 이하**를 권장합니다. (너무 크면 홈페이지가 느려집니다)
- 파일 이름은 **영문·숫자·하이픈(-)** 만 사용합니다. (한글·공백·괄호 ❌)
  - 좋은 예: `work-4.jpg`, `hero-2.jpg`
  - 나쁜 예: `사진 (1).jpg`, `현장1.JPG`
- 사진 위에는 글자·로고·박스를 넣지 않은 **깨끗한 원본 사진**을 사용합니다.

---

## 11. GitHub 업로드 방법 (수정 내용을 실제 홈페이지에 반영)

이 홈페이지는 GitHub 라는 곳에 올려져 자동으로 공개됩니다.
수정한 내용을 반영하려면 GitHub 에 올리면 됩니다.

### 글을 고쳤을 때 (`data/site-content.js`)
1. 인터넷에서 저장소 주소로 접속: **github.com/nam2037772/ainsafe-site**
2. `data` 폴더 → `site-content.js` 파일을 클릭합니다.
3. 오른쪽 위 **연필(✏️ Edit) 아이콘**을 누릅니다.
4. 내용을 고칩니다.
5. 아래 초록색 **Commit changes** 버튼을 누릅니다.
6. 1~2분 뒤 홈페이지에 반영됩니다.

### 사진을 올릴 때
1. 저장소에서 `assets` → `images` → 원하는 폴더(`hero`/`about`/`works`)로 들어갑니다.
2. **Add file → Upload files** 를 누릅니다.
3. 사진을 끌어다 놓고 **Commit changes** 를 누릅니다.
   - 같은 이름으로 올리면 기존 사진이 교체됩니다.

---

## 12. 문제가 생겼을 때 복구 방법

### 화면이 깨지거나 내용이 안 보일 때
대부분 `site-content.js` 에서 **쉼표(,)** 나 **작은따옴표(')** 가 빠진 경우입니다.
1. 방금 고친 부분을 다시 확인합니다. (따옴표·쉼표 짝이 맞는지)
2. 잘 모르겠으면 아래 방법으로 **이전 상태로 되돌립니다.**

### 이전 상태로 되돌리기 (GitHub)
1. github.com/nam2037772/ainsafe-site 접속
2. 위쪽 **Commits**(시계 모양/커밋 목록)를 클릭합니다.
3. 문제가 생기기 **직전 항목**을 찾아 들어갑니다.
4. 오른쪽 **`...` → Revert** 또는 그 시점 파일을 복사해 다시 저장합니다.

### 홈페이지 전체를 예전 디자인으로 되돌리기
- 프로젝트 안에 `index.legacy.html` 이 **예전 홈페이지 백업**으로 남아 있습니다.
  필요하면 개발자에게 “`index.legacy.html` 로 되돌려 달라”고 요청하면 됩니다.

---

## 요약

| 하고 싶은 일 | 고칠 곳 |
|---|---|
| 글·회사정보·시공사례 수정 | `data/site-content.js` (이 파일 하나) |
| 사진 교체 | `assets/images/` 안 폴더에 같은 이름으로 업로드 |
| 실제 반영 | GitHub 에서 Commit |

> **홈페이지 제작은 개발자가, 운영은 사장님이 직접.**
> 막히면 이 문서의 12번(복구)을 먼저 확인하세요.
